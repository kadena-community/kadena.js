import base64UrlEncode from '../base64UrlEncode';

test('Takes in a string and outputs a Base64 Url encoded string', () => {
  const str = '';

  const actual = base64UrlEncode(str);
  const expected = '';

  // expect(expected).toEqual(actual);
});
