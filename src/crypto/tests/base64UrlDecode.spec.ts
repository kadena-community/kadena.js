import base64UrlDecode from '../base64UrlDecode';

test('Takes in a Base64 Url encoded string and outputs decoded string', () => {
  const str = '';

  const actual = base64UrlDecode(str);
  const expected = '';

  // expect(expected).toEqual(actual);
});
