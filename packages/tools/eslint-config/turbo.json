{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["mixins/**", "profile/**"],
      "outputMode": "new-only"
    }
  }
}
