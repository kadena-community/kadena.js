// function throwErrorIfUnsigned(command: ICommand): command is ICommand {
//   if (!isSignedCommand(command)) throw new Error('Command is not fully signed');
//   return isSignedCommand(command);
// }
