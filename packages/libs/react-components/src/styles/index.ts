export {
  styled,
  css,
  config,
  theme,
  darkTheme,
  getCssText,
  createTheme,
  globalCss,
  sizes,
} from './stitches.config';
export * from './types';
export { baseGlobalStyles } from './baseGlobalStyles';
