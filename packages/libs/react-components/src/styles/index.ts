export {
  styled,
  css,
  config,
  theme,
  darkTheme,
  getCssText,
  createTheme,
  globalCss,
  sizes,
  keyframes,
  IThemeColors,
} from './stitches.config';
export { baseGlobalStyles } from './baseGlobalStyles';
