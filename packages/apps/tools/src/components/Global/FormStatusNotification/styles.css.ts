import { sprinkles } from '@kadena/react-ui/styles';
import { style } from '@vanilla-extract/css';

export const containerStyle = style([
  sprinkles({ marginY: '$6', width: '100%', maxWidth: '100%' }),
]);
