import React, { FC } from 'react';
import { styled } from '@kadena/react-components';

const StyledHeader = styled('header', {
  backgroundColor: '#1D1D1F',
});

export const Header: FC = () => {
  return <StyledHeader>sdf</StyledHeader>;
};
