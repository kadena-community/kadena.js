import { Icons } from '@kadena/react-components';

import React, { FC } from 'react';

export const HamburgerMenuToggle: FC = () => {
  return (
    <button>
      <Icons.MenuOpenIcon />
    </button>
  );
};
