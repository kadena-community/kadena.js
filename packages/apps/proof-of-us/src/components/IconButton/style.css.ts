import { style } from '@vanilla-extract/css';

export const buttonClass = style({
  border: 0,
  background: 'transparent',
  color: 'white',
  cursor: 'pointer',
  padding: '10px',
  paddingInlineEnd: 0,
});
