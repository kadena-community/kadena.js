{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "dist",
        "fp",
        "lib",
        "theme",
        "types",
        ".next",
        "!.next/cache/**"
      ],
      "outputMode": "new-only"
    },
    "test": {
      "dependsOn": ["build"],
      "inputs": ["src/**/*.ts", "src/**/*.tsx"],
      "outputMode": "new-only"
    },
    "lint": {
      "outputMode": "new-only"
    },
    "format": {
      "outputMode": "new-only"
    },
    "format:ci": {
      "outputMode": "new-only"
    }
  }
}
