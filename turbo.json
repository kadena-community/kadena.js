{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist", "lib"],
      "outputMode": "full"
    },
    "test": {
      "dependsOn": ["build"],
      "inputs": ["src/**/*.ts", "src/**/*.tsx"],
      "outputMode": "full"
    },
    "lint": {
      "dependsOn": ["build"],
      "outputMode": "full"
    },
    "format": {
      "outputMode": "full"
    },
    "format:ci": {
      "outputMode": "full"
    }
  }
}
